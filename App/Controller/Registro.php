<?php

$dados = $_POST;

// chama conexao
// pega dados
// faz envio
// redireciona

// UPDATE DE IMAGEM: UNLINK

// testando aquivo
// ADICIONAR ENCTYPOE MULTIDATA PARA IMAGENS

/*
     $aquivo = $_FILE['nomeInput'];
    if($arquivo !== null){
        preg_match("/\.(png|jpg|jpeg){1}$/1", $arquivo['name'], $ext);

        if($ext == true){
            $nome_arquivo = md5(uniqid(time())) . '.' . $ext[1]
            $caminho_arquivo = "img/" . $nome_arquivo;
            move_uploaded_file($aqruivo['tmp_name'], $caminho_arquivo);

            $sql = "faz sql insert"

            $inserir = execute();
        }
    /}

    header(location)
    */